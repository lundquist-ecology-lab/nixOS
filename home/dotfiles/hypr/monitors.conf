# Monitor configuration
# Start with simple configuration to avoid freeze on startup
# HDR and color management will be applied after initialization via autostart script
monitor = DP-6,3840x2160@160.00,0x0,1.25
monitor = HDMI-A-2,1920x1080,-1080x-252,1
